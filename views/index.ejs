<%-include('partials/header')%>
  <body>
    <%-include('partials/navbar')%>


    <div class="container">
        <h1 class="perguntas-titulo">Perguntas</h1>
        <a href="/perguntar" class="btn btn-primary">Perguntar</a>
        <br>
      <%pergunta.forEach(perguntas =>{%>
        <br>  
        <div class="card">
          <div class="card-body">
            <h3 class="perguntas-titulo"><%= perguntas.titulo %></h3>
          </div>
          <div class="card-footer">
              <a class="btn btn-primary" href="/pergunta/<%= perguntas.id %>">Responder</a>
          </div>
        </div>
     <%})%>

    
    </div>

    
  </body>
<%-include('partials/footer')%>